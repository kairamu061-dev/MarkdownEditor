# editor/live-preview テストケース

## ユニットテスト

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （なし — 表示中心のため E2E で担保） | | | |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | 型チェック・ビルド | npm install 済み | `npm run build` | エラーなく完了 | [x] |

## E2Eテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | 整形表示（カーソル外） | スクラッチ文書表示 | 起動直後の表示を確認 | カーソル行（H1）以外の記法マークが隠れる。箇条書きは • （アクセント色）、リンクはテキストのみ | [x] |
| E-02 | ソース表示（カーソル内） | E-01 実施後 | 太字部分をクリック | `**` マークが再表示される。同時にカーソルが出た H1 の `#` は隠れる | [x] |
| E-03 | 再隠蔽 | E-02 実施後 | カーソルを太字の外へ移動 | `**` が再び隠れる | [x] |
| E-04 | 編集への非干渉 | E-01 実施後 | 文字入力 → Ctrl+Z → Ctrl+Y | 置換表示があっても入力・undo/redo が正常動作 | [x] |
| E-05 | Windows 11 実機 | Windows 実機 | E-01〜E-04 を実施 | 同一挙動 | [x] |
| E-06 | テーブルレンダリング | GFM テーブルを含む文書 | テーブルからカーソルを外す / テーブルをクリック | カーソル外: `<table>` として描画（ヘッダー bold + accent-secondary・ボーダーあり）。クリックでソース表示に戻る | [x] |
| E-07 | テーブル内インラインコード・リンク | テーブルセルに `` `code` `` と `[text](note.md)` を含む文書 | テーブルからカーソルを外す | コードスパンがモノスペース+背景色、リンクがアクセント色+下線で描画される | [x] |
| E-08 | インラインリンクのクリックナビゲーション | 複数ノートが存在する保管庫、`[リンク](other.md)` を含むノートを開く | カーソルをリンクの外に出してリンクをクリック | `other.md` が開く。ツリーの強調が追随する（BUG-012 修正。太字入り `[**x**](y.md)` も可） | [x] |
| E-09 | インラインリンク ソース表示 | E-08 と同じ文書 | カーソルをリンク範囲内へ移動 | `[リンク](other.md)` の全記法が表示され編集できる | [x] |
| E-10 | 引用文 Backspace 正常動作 | `> 引用テキスト` を含む文書 | 引用行の中央にカーソルを置き Backspace を押す | カーソル直前の 1 文字のみが削除される（BUG-008 修正確認） | [x] |
| E-11 | 箇条書きハンギングインデント | 長い文字列を含む箇条書きノート（0 段目のみ） | ウィンドウ幅を狭め、テキストを折り返させる | 折り返し行が「• 」の右端に揃い、左端に戻らない | [x] |
| E-12 | 箇条書き深いネスト（10段） | スクラッチ文書 | `- l1` → Enter → Tab → `l2` → Enter → Tab → … と繰り返し 10 段まで入力 | 原文は各レベル 2sp ずつだが、**見た目**は 1 段あたり固定幅（`--md-list-indent-step`）で送られる。Enter 後も同一レベルが継続する | [x] |
| E-13 | 引用ブロックの見た目 | `> 引用` を含む文書（複数行の引用も） | 引用行の表示を確認 | 各引用行に左の縦バー（quote-bar）＋淡い背景（quote-bg）が付き、本文が落ち着いた色（quote-text）の斜体になる。複数行の引用ではバー・背景が縦に繋がる。行頭の `>` マークは隠れる（`**強調**`・`` `code` `` は読める）。引用でない通常行・箇条書きにはバー/背景が付かない | [x] |
| E-14 | 引用マークのソース表示と `>` 無し行の非引用化 | `> 引用` を含む文書 | (a) 引用行にカーソルを置く (b) 引用行の次行の `>` を消して本文を書く | (a) カーソルのある行だけ `>` がソース表示され編集できる（他の引用行の `>` は隠れたまま）。(b) `>` を消した行は引用扱いにならず、バー/背景/本文色に加え**斜体も付かない**普通の段落になる（CommonMark の遅延継続に引きずられない・BUG-011） | [x] |
| E-15 | 引用の Enter 継続が行頭 `>` に限定される（BUG-011） | スクラッチ文書 | (a) `> quote` → Enter → Backspace で `> ` を消す → 通常文を入力 → 行末で Enter (b) `> abc` の行末で Enter | (a) `>` を消した通常行で Enter しても次行は**通常行**（`> ` が挿入されない）。(b) 行頭が `>` の行では従来どおり `> ` が継続する。箇条書き（`- `）の継続も壊れない | [x] |
| E-16 | インラインリンクのサブフォルダ指定（BUG-019） | `sub/note.md` が存在する保管庫 | `[リンク](sub/note.md)` を書いてクリック | `sub/note.md` が開く（フォルダを捨てず解決＝ウィキリンク `[[sub/note]]` と同一挙動） | [x] |
| E-17 | 0 段目の行頭余白 | `- 項目` と通常の段落を並べた文書 | 両方の行の左端を見比べる | 箇条書きの `•` が通常の段落より少し右から始まる（ぶら下がりではなく行そのものが右へ寄る） | [x] |
| E-18 | 段ごとのインデント深さ | 3 段までネストした箇条書き | 各段の `•` の横位置を見る | 1 段ごとに一定幅で深くなる。変更前（原文 2sp 相当）より明らかに深い | [x] |
| E-19 | ネストした行の折り返し位置 | 2 段目・3 段目に長文を含む箇条書き | ウィンドウ幅を狭めて折り返させる | 折り返し行の左端が、その項目の**本文開始位置**（`•` の右）にぴったり揃う。段が深くても左へずれない（E-11 の全段版）。ただし**1 語が行幅より長い**場合（スペース無しで打ち続けた文字列など）は、1 行目が弾丸だけになり本文が次の視覚行から始まる — CSS の行分割由来の既知の制限で対象外（dev-notes 参照） | [x] |
| E-20 | 段の縦ガイド線 | 3 段までネストした箇条書き | 表示を確認 | 段 1 以降の行に、祖先の段ごとに 1 本ずつ細い縦線がうっすら表示される。連続する行で縦に繋がって見える。0 段目だけの箇条書きには線が出ない | [x] |
| E-21 | カーソル出入りで行が横に飛ばない | ネストした箇条書き | 箇条書き行にカーソルを入れる / 出す | `-` のソース表示と `•` が切り替わっても、行の左端と `-`／`•` の横位置が動かない。上下カーソル移動で段をまたいでもガタつかない | [x] |
| E-22 | 空の箇条書き項目を Enter で閉じる（BUG-025） | スクラッチ文書 | (a) `- aaaa` → Enter → Enter (b) `- a` → Enter → Tab → `b` → Enter → Enter (c) `1. a` → Enter → Enter | (a) 2 回目の Enter で `- ` が消え、**空行は挿入されない**（`- aaaa` の次の行にカーソル）。(b) 1 段浅い `- ` になる（空白だけの行が残らない）。(c) `1. a` だけが残り `2. ` は生成されない | [x] |
| E-23 | 番号付きリストのインデント表示 | `1.` を 3 段までネストした文書（`9.` `10.` `100.` も含む） | 表示を確認 | 箇条書きと同じ深さ・同じ縦ガイド線が付く。0 段目にも行頭の余白がある。折り返し行が本文の開始位置に揃う。`10.` `100.` でも番号が本文に重ならない。カーソルの出入りで位置が動かない | [x] |
| E-24 | リスト項目の Tab / Shift+Tab と採番（BUG-026） | 番号付きリストを含む文書 | (a) `1./2./3.` の 2 番目で Tab (b) その状態で Shift+Tab (c) `3.` 始まりのリストの 2 番目で Tab (d) Tab のあと Ctrl+Z 1 回 (e) 箇条書きで Tab | (a) `1. a` / `   1. b` / `2. c` — 3 スペースで入れ子になり、入れ子は 1 から、後続は繰り上がる。(b) 元の `1./2./3.` に戻る。(c) 最上位は `3.` `4.` のまま、入れ子だけ `1.`。(d) **1 回で**字下げごと完全に元に戻る。(e) 従来どおり 2 スペースで入れ子、番号は関与しない | [x] |
| E-25 | 遅延継続行の Enter で文字が消えない（BUG-027） | スクラッチ文書 | (a) `- aaaa` → Enter → Enter → `a` → Enter (b) `1. aaaa` で同じ操作 (c) `- item one` → Enter → Backspace → `  continued` → Enter | (a)(b) 入力した `a` が**残り**、次の行にカーソルが移る。(c) 桁より右に本文がある継続行は従来どおりの継続動作 | [x] |
| E-26 | 番号付きの IME 変換中の表示（BUG-028） | スクラッチ文書 | `1. ` を入力し、続けて日本語を変換中の状態にする | 未確定の文字が `1. ` の**右隣**に出る。一行下に落ちない・行の高さが増えない。確定後も正しい位置 | [x] |
| E-27 | 遅延継続行が字下げされない（BUG-029） | スクラッチ文書 | `- aaaa` → Enter → Enter → `a` | `a` が通常の段落と同じ左端から始まる（弾丸の右へ寄らない） | [x] |
| E-28 | インデント上のカーソル移動（BUG-030） | 3 段までネストしたリスト | 3 段目の行末から ← を連打する | 行頭空白の上でも等間隔に動く。抜ける瞬間に大きく飛ばない | [x] |
| E-29 | 空のリスト項目の Backspace（BUG-031） | スクラッチ文書 | `- aaaa` → Enter → Backspace | 完全な空行になり、カーソルが行頭に来る。空白が残らない（保存して開き直しても残っていない） | [x] |
| E-30 | 番号と本文の間隔（BUG-032） | `1.` `10.` `100.` を含む文書 | 番号と本文の間隔を見る | 原文どおりの自然な間隔。1 文字ぶんの隙間が空かない。折り返し行は本文の開始位置に揃ったまま | [x] |

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格

---

実施記録: I-01 / E-01〜E-04 は 2026-07-05 に Xvfb + xdotool でリリースバイナリに対して実施し合格（スクリーンショット確認）。
E-05 は 2026-07-05 に Windows 11 実機（dev モード）で確認し合格。
E-06〜E-12 は 2026-07-17 の機能追加に伴い追加。2026-07-20 の Windows 11 実機検証（`9ccc41a`）で E-06〜E-11 を確認し合格。
E-08（インラインリンクのクリック遷移）は 2026-07-20 に合格と記録したが、2026-07-26 に**遷移が全く機能していない**ことが判明（`data-href` が未付与＝偽陽性だった）。BUG-012 として起票・修正し、2026-07-27 の Windows 実機検証（`a8e86b7`）で実マウス遷移（通常・太字入り・外部非遷移・ウィキリンク回帰）を確認し合格。
E-12（深いネスト 10 段の Tab タイプ手順）は 2026-07-24 にユーザーが Windows 実機で確認し合格。
E-13 は 2026-07-22 の引用ブロック装飾追加に伴い追加。2026-07-24 に「`>` の見た目を消したい」「行頭が `>` でない行は引用扱いしない」とのフィードバックを受けて装飾方式を Blockquote ノード単位から QuoteMark 単位に変更（期待結果も更新）。2026-07-26 の Windows 実機検証（`93819cf`）で E-13/E-14 とも合格。
E-14 は 2026-07-24 の上記フィードバック対応に伴い追加。2026-07-26 の Windows 実機で合格。ただし遅延継続行で「斜体だけ残る／Enter で `> ` 復活」の取りこぼしが判明し BUG-011 として起票・修正（期待結果に「斜体も付かない」を追記）。
E-15 は BUG-011（Enter 継続の QuoteMark 基準化・斜体の QuoteMark 基準化）の確認用に 2026-07-26 追加。開発機 headless Chromium で実キー入力（Playwright）＋computed font-style により確認後、2026-07-27 の Windows 実機検証（`a8e86b7`）で合格。
E-16 は BUG-019（インラインリンクがフォルダを捨てていた・コードレビュー §3）修正（`118e0a3`）の確認用に追加。解決ロジック（`openNoteByName` のパス照合）は BUG-015 で確認済み。2026-08-06 の Windows 実機検証（`4e371bc`）で、ルートに同名 `note.md` がある状態でも `sub/note.md` が開くことを確認し合格。
E-17〜E-21 は 2026-08-19 の箇条書きインデント表示の追加に伴い追加。初回実装は実機で「`•` の位置が違う」（弾丸が段によらず行頭に貼り付く）と判明し、`text-indent` の継承が原因と特定して修正（dev-notes 参照）。修正後、開発機の headless Chromium（Playwright + esbuild で `src/editor` を直接バンドル）で E-17〜E-21 相当を実測確認済み — 弾丸の実描画位置が 1 段 4ch ずつ送られること、折り返し行の x が本文開始の x と 0 段目・1 段目とも一致すること、カーソルの出入りで行の左端が動かないこと。2026-08-19 にユーザーが修正後の Windows ビルドで確認し、E-17 / E-18 / E-20 / E-21 は合格。E-19 は当初「ローマ字だと崩れる」と報告されたが、追試の結果**スペースを空けずに 1 行ぶん打ち続けていたため**と判明し、単語ごとにスペースを空けた普通の文章では意図どおりと確認された。1 語が行幅を超える場合に 1 行目が弾丸だけになるのは CSS の行分割由来（`overflow-wrap: anywhere`）で、v0.3.0 と同じコードでも再現する。既知の制限として許容し合格扱いとした。E-27〜E-30 は 2026-08-20 の報告 4 件（BUG-029〜032）の確認用に追加。E-28 は ← を 22 回押したときのカーソル x を実測し、行頭空白 4 文字ぶんの移動量が 4px×4 + 54px の飛び → 18px×4 の均等になることを確認した。E-27〜E-30 は 2026-08-20 の Windows 検証（下記）で合格。
E-25 / E-26 は 2026-08-20 の報告 2 件（BUG-027 / BUG-028）の確認用に追加。E-26 は CDP の `Input.imeSetComposition` で実際の変換中状態を作り、行の高さと文字の描画位置を実測して確認した。どちらも 2026-08-20 の Windows 検証（下記）で合格。E-26 は実機の IME を実際に ON にした状態で確認されている。
E-23 / E-24 は 2026-08-20 の番号付きリスト対応（要望 + BUG-026）に伴い追加。開発機の headless Chromium で実キー入力 12 ケースと寸法実測により確認し、2026-08-20 の Windows 検証（下記）で合格。E-24 の採番規則（入れ子は常に 1・最上位は先頭項目の番号を引き継ぐ）は、規則を先に決めずに 12 ケースの出力から決めた — 当初案は入れ子リストが分割されるケースで破綻した。
E-22 は BUG-025（空項目の Enter がマークを消さない）の確認用に 2026-08-19 追加。修正前後を実キー入力で比較し、引用まわり 3 ケース（E-15 相当を含む）が 1 バイトも変わらないことも確認済み。2026-08-19 にユーザーが修正後の Windows ビルドで確認し合格（BUG-025 Closed）。
**2026-08-20 の Windows 検証（`568baaa` 相当のビルド）で E-23〜E-30 を全項目合格として `[x]` にした。**
実施したのは**ユーザーではなく Windows 側の自動化セッション**で、実機（Windows 11 / WebView2）を
`scripts\verify-build.bat` で再ビルドして起動し、UI Automation による実キー入力・スクリーンショットの目視・
`cat -A` / `xxd` によるディスクのバイト照合で確認している（結果は `tmp/フィードバック/old/` に保管）。
最も念入りに見るよう依頼した E-26（番号付きの IME）は、実機の IME を半角/全角キーで実際に ON にし、
変換中の下線をスクリーンショットで確認したうえで一段目〜三段目を確認したと報告されている。
その後 **2026-08-21 にユーザー自身が実機で確認し、全項目問題なしと報告**（IME を含む）。
これを受けて BUG-026〜032 を `Closed` にした。

同じ変更で E-11 / E-12 の期待結果も書き換えた（`[x]` は維持）。E-11 は前提を「0 段目のみ」に限定した — 全段での折り返し一致は新設の E-19 が受け持つ。E-12 は「各レベルが 2sp ずつインデントされ」が**原文**の話なのか**見た目**の話なのか曖昧だったため、原文は 2sp のまま・見た目は固定幅と明記した。合格の実体（10 段まで入力でき、Enter で同じ段が継続する）は変わっていないので再検証は不要と判断した。
