# settings/ui 開発メモ

## 実装上の判断

| 判断内容 | 理由 |
|----------|------|
| 保存は save_editor_settings（部分更新）を使用 | 起動時に取得した Settings をそのまま save_settings すると、セッション中に開いた別保管庫の lastVault を古い値で上書きする競合があるため。Rust 側でファイルを読み直して editor のみ差し替える |
| フォント適用は CSS カスタムプロパティ | editor/core の theme.ts を「変数参照 + フォールバック」の 1 箇所変更に留められる（core の再ビルド的変更なし） |
| fontSize がデフォルト値（14）のときは null で保存 | settings.json を最小に保ち、将来デフォルトを変えたときに追随できる |
| ヘッダ内ボタンの並び順は flexbox の order で固定 | ⚙ と [+] は別モジュールが DOM 挿入するため、挿入順に依存すると並びが不安定になる |

## 発生した問題と対処

| 問題 | 対処 |
|------|------|
| ⚙ ボタンがタイトル横（左側）に表示された | initSettings が initExplorer より先に走り DOM 挿入順が仕様と逆になったため。explorer.css / settings.css に order を追加して表示順を固定 |

## 設計からの変更点

| 変更内容 | 理由 |
|----------|------|
| ⚙ ボタンをサイドバーヘッダから下部フッタ（保管庫スイッチャの横・右端）へ移設（2026-07-12） | ユーザーフィードバック「設定ボタンは保管庫切り替えの横に」。旧サイドバーヘッダ自体を撤去（フレームレス化・「ファイル」表示廃止）したため。order による並び順制御は不要になり撤去。フッタは vault-switch のスイッチャと同居するため、`renderVaultSwitcher` はスイッチャ部分のみ差し替えて ⚙ を消さないようにした |
| フォントファミリーを自由入力（text）からプリセット選択（select）へ変更（2026-07-12） | ユーザーフィードバック「いくつかの選択肢から選ぶ形にして」。選択肢は Windows 11 標準搭載フォント + フォールバック付き CSS 値（spec.md の表）。保存形式は不変のため store / 適用処理は無変更。プリセット外の保存済み値は「カスタム（現在の設定）」option で保持。存在しないフォント名が入力されうる問題（下記・今後の課題に記載していた件）もこれで解消 |

## 今後の課題

- 入力中のライブプレビュー、Esc/キャンセル系の E2E（test-cases.md E-04）
- 設定項目の追加（テーマ・自動保存間隔など）に備えたモーダルのセクション化
- ~~フォントファミリーは自由入力のため、存在しないフォント名のフォールバック挙動が未確認~~
  → 2026-07-12 のプリセット選択化で解消（全選択肢にフォールバックを明示）

## ユーザへの要望

- Windows 11 実機での確認（test-cases.md E-05）
