# settings/ui テストケース

## ユニットテスト

（なし — UI 中心のため E2E で担保）

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | 型チェック・ビルド | npm install 済み | `npm run build` | エラーなく完了 | [x] |

## E2Eテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | モーダル表示 | 起動済み | ⚙ をクリック | オーバーレイ + モーダル（サイズ 14・ファミリー空欄の初期値） | [x] |
| E-02 | 保存と即反映 | E-01 実施後 | サイズ 20 / ファミリー serif → 保存 | エディタ本文のみ 20px セリフ体に変わる。settings.json に保存され lastVault は保持 | [x] |
| E-03 | 再起動後の維持 | E-02 実施後 | 再起動 | フォント設定が適用された状態で起動 | [x] |
| E-04 | 破棄 | E-01 実施後 | 値変更 → キャンセル / Esc / 外側クリック | 反映されず閉じる | [x] |
| E-05 | Windows 11 実機 | Windows 実機 | E-01〜E-04 | 同一挙動 | [x] |
| E-06 | フォントの選択式化 | 2026-07-12 変更後 | ⚙ → ファミリー select でプリセット（例: 游明朝）を選択 → 保存 | エディタ本文が選択フォントに変わり settings.json にフォールバック付き CSS 値が保存される。既定は「システムフォント」 | [x] |
| E-07 | プリセット外の旧設定値の保持 | settings.json の fontFamily に旧・自由入力値 | ⚙ を開く | 「カスタム（現在の設定）」が選択状態。別のプリセットで保存すると置き換わる | [x] |

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格

---

実施記録: I-01 / E-01〜E-03 は 2026-07-05 に Xvfb + xdotool でリリースバイナリに対して実施し合格
（スクリーンショットと settings.json で確認）。
E-04 / E-05 は 2026-07-05 に Windows 11 実機（dev モード）で確認し合格
（破棄は Esc で実測。キャンセル / 外側クリックは同一の close 経路のため同等と判断）。
E-06 / E-07 は 2026-07-12 に Xvfb + xdotool でリリースバイナリに対して実施し合格。
游明朝の選択で settings.json に `"Yu Mincho", "游明朝", serif` が保存され本文がセリフ体
（Linux では末尾フォールバック）に変化。プリセット外の値（Comic Sans MS）は「カスタム（現在の設定）」
として保持され、システムフォントで保存し直すと null に置き換わることを確認。
各フォントの実際の見た目（游ゴシック / BIZ UD 系）は Windows 実機での確認が必要。
