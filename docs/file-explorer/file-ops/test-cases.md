# file-explorer/file-ops テストケース

## ユニットテスト

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （なし — UI 中心のため E2E で担保） | | | |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | 型チェック・ビルド | npm install 済み | `npm run build` | エラーなく完了 | [x] |

## E2Eテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | ルート直下への新規ノート作成 | 保管庫を開いた状態 | ツリーの空白部を右クリック →「新規ノート」 | 「無題のノート.md」がルート直下に作成され、開かれる | [x] |
| E-02 | 連番付与 | E-01 実施後 | 同じ操作をもう一度 | 「無題のノート 2.md」が作成される | [x] |
| E-03 | コンテキストメニュー | ノートあり | ノート行を右クリック | リネーム / 削除 のメニューが表示される | [x] |
| E-04 | リネーム | E-03 実施後 | リネーム → 名前入力 → Enter | ディスク上でリネームされ、ツリーに反映される | [x] |
| E-05 | 二段階削除 | E-03 実施後 | 削除 →「本当に削除する」 | 2 回目のクリックで初めて削除され、ツリーから消える | [x] |
| E-06 | 編集中ノートの削除 | ノートを開いた状態 | そのノートを削除 | エディタが空になり保存対象なしに戻る | [x] |
| E-07 | Windows 11 実機 | Windows 実機 | E-01〜E-06 | 同一挙動（削除がごみ箱に入ることも確認） | [x] |
| E-08 | フォルダ内への新規ノート | フォルダあり | フォルダ右クリック →「新規ノート」 | そのフォルダ内に作成され、開かれる | [x] |
| E-09 | 新規フォルダ作成 | 保管庫を開いた状態 | 右クリック →「新規フォルダ」 | フォルダが作成されツリーに表示される | [x] |
| E-10 | フォルダの二段階削除 | フォルダあり | 削除 →「フォルダごと削除する」 | 中身ごとごみ箱へ移動しツリーから消える | [x] |
| E-11 | フォルダのリネーム | フォルダあり | 右クリック → リネーム → Enter | ディスク上でリネームされ、配下ノートのパスが追随する | [x] |
| E-12 | フォルダ配下の開いているノートのリネーム追随（BUG-013） | フォルダ配下のノートを開いた状態 | 親フォルダをリネーム → 開いているノートを編集 | 「保存に失敗しました」が出ず、編集が**新パス**に保存される（開いているノートの currentPath が追随する） | [x] |
| E-13 | リネーム入力の blur 確定（BUG-016） | ツリー項目をリネーム中 | 新しい名前を入力し、Enter を押さずに画面外をクリック | 妥当で変更ありなら**確定される**（入力が黙って破棄されない）。空/無変更/不正名なら元のまま | [x] |

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格

---

実施記録: I-01 / E-01〜E-05 は 2026-07-05 に Xvfb + xdotool でリリースバイナリに対して実施し合格
（スクリーンショットとディスク上のファイルで確認）。
E-06 / E-07 は 2026-07-05 に Windows 11 実機（dev モード）で確認し合格
（ごみ箱への投入を Shell.Application で確認。リネームの日本語名も正常）。
E-08〜E-10 は 2026-07-09 に Windows 11 実機（release exe）で確認し合格
（フォルダ削除は中身ごとごみ箱に入ることを origin パスで確認）。
E-11（フォルダのリネーム）は 2026-07-30 の Windows 実機検証（`fccff3b`）で、ツリー/ディスク改名・配下ノートのパス追随（ツリー上）・改名後の配下ノート編集保存を確認し合格。
なお同検証で「配下ノートを**開いた状態**での親フォルダ改名時に保存不能になる」不具合を検出（BUG-013）→ E-12 を追加し `remapAfterRename` で修正（`2ecbe00`）。
E-13 は blur 確定化（BUG-016）の確認用に追加。E-12/E-13 とも 2026-08-01 の Windows 実機検証（`26d72d7`）で合格（回帰＝ノート rename・DnD 移動のパス追随・Esc・衝突ブロックも確認）。

E-01 / E-02 は 2026-07-05 の実施時点ではサイドバーヘッダの `[+]` ボタンで確認したもの。
同ボタンは 2026-07-12 のフレームレスタイトルバー化で撤去されたため、手順を現在の導線
（空白部の右クリック）に書き換えた。作成処理そのものは同じ `createNote` 経路で、
2026-07-09 の E-08〜E-10（コンテキストメニュー経由）で実機確認済み。
