# file-explorer/file-ops タスク

## 実装タスク一覧

<!-- ステータス: [ ] 未着手 / [~] 進行中 / [x] 完了 -->

- [x] index.ts への公開フック追加（refreshTree / getCurrentPath / setCurrentPath）
- [x] [+] ボタンと連番付き新規作成
- [x] コンテキストメニュー（リネーム / 二段階削除）
- [x] インラインリネーム入力と編集中ノートへの追随
- [x] E2E 検証（作成 → リネーム → 削除 をスクリーンショットとディスクで確認）
- [x] 新規フォルダ作成（`create_folder`）とメニューからのフォルダ/ノート作成（作成先ディレクトリ対応）
- [x] フォルダのリネーム・削除（中身ごとごみ箱へ）に対応
- [x] ツリー空白部（ルート）右クリックからの作成メニュー
- [ ] Windows 11 実機でのフォルダ作成・削除・メニュー確認

## 依存関係

- file-explorer/tree-view の完了が前提
- フック追加 → 新規作成 / メニュー → リネーム / 削除 → 検証
- フォルダ作成/削除は vault の `create_folder` / 汎用化した `delete_path` に依存
