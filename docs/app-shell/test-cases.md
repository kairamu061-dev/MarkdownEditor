# app-shell テストケース

## ユニットテスト

| ID | テスト名 | 入力 / 条件 | 期待結果 | ステータス |
|----|---------|------------|---------|-----------|
| U-01 | （なし — app-shell はロジックが薄いため手動 E2E のみ） | | | |

## インテグレーションテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| I-01 | フロントエンドビルド | npm install 済み | `npm run build` | tsc / vite build がエラーなく完了 | [x] |
| I-02 | リリースビルド | Rust ツールチェーン導入済み | `npm run tauri build -- --no-bundle` | release バイナリが生成される | [x] |

## E2Eテスト

| ID | テスト名 | 前提条件 | 手順 | 期待結果 | ステータス |
|----|---------|---------|------|---------|-----------|
| E-01 | 起動と初期表示 | ビルド済みバイナリ | アプリを起動 | Nord 配色で左サイドバー（nord1）+ メイン領域（nord0）の 2 ペインが表示される | [x] |
| E-02 | サイドバー開閉（Ctrl+B） | E-01 実施後 | `Ctrl+B` を押す | サイドバーが非表示になり、トグルボタンは左上に残る。再度押すと表示に戻る | [x] |
| E-03 | サイドバー開閉（ボタン） | E-01 実施後 | 左上トグルボタンをクリック | E-02 と同じ挙動 | [x] |
| E-04 | Windows 11 実機起動 | Windows 実機 + MSI/NSIS ビルド | インストールして起動 | E-01〜E-03 と同じ挙動 | [~] |

## ステータス凡例

- `[ ]` 未実施
- `[~]` 実施中
- `[x]` 合格
- `[!]` 不合格

---

実施記録: I-01 / I-02 / E-01 / E-02 は 2026-07-04 に Linux devcontainer（Xvfb + xdotool）で実施し合格。
E-03 は 2026-07-05 に Windows 11 実機（dev モード）で確認し合格。
E-04 は dev モードでの起動・表示・サイズ設定値までは実機確認済み。MSI/NSIS からのインストール起動と、
マウスドラッグでの最小サイズ（640x480）制限の確認が残（ユーザーの手動確認待ち）。
